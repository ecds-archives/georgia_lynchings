Django
httplib2
# lxml # Was part of the events app, no longer needed.
mysql-python # HACK: we don't strictly depend on mysql. find a better way to install it in deploy.
# rdflib>=3.0 # Was part of events app, no longer needed.
south==0.7
